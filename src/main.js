import React from 'react'
import ReactDOM from 'react-dom/client' // allows us to use the DOM
import App from './App' // allows App component to be used
import './index.css'

import { createBrowserRouter, RouterProvider } from 'react-router-dom';

